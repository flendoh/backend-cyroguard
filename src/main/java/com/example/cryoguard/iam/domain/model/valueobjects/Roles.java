package com.example.cryoguard.iam.domain.model.valueobjects;

/**
 * Roles enum for CryoGuard IAM
 * <p>
 *     This enum represents the roles in the CryoGuard system.
 * </p>
 */
public enum Roles {
    ROLE_ADMINISTRATOR,
    ROLE_OPERATOR
}